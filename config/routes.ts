// config/routes.ts
// Public and protected route definitions.

export const ROUTES = {
  home: "/",
  dashboard: "/dashboard",
  settings: "/settings",
  inbox: "/inbox",
  alerts: "/alerts",
  billing: "/billing",
  admin: "/admin",
  rules: "/rules",
  analytics: "/analytics",
  integrations: "/integrations",
} as const;
