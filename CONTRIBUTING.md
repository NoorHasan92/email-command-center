# Contributing to Inbox Sentinel

1. Fork the repo and create your branch from `main`.
2. Run `npm install`.
3. Create a `.env` based on `.env.example`.
4. Ensure `npm run lint` and `npm run build` pass.
5. Create a PR.
